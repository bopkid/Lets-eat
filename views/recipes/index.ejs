
<link rel="stylesheet" type="text/css"   href="/css/index-recipe/style.css">




    <h1>Recipes Page</h1>

        <section class = "recipe-index">
            <%recipe.forEach((recipes) => {%> 
 
                <section class = "recipe-container">
            <span class = "recipe-container__name"><h4>Name:</h4>
                <p><a href="recipes/<%=recipes._id%>">
                    <%= recipes.name %> 
                </a></p>
            </span>
            <span class = "recipe-container__origin">
            <h4>Origin:</h4><p> <%=recipes.origin%> </p>
        </span>
        <span class = "recipe-container__ingredients">
            <h4>Ingredients:</h4><p> <%=recipes.ingredients%> </p>
        </span>
            <span class = "images">
            <img src = "<%= recipes.url %>" alt="<%=recipes.name%> ">
        </span>
        <span class = "recipe-container__ratings">
            <h4>Ratings:</h4><p><%=recipes.avgRating%></p>
        </span>
        <br>
        <section class = "recipe-container__ratings--add">
          <form action="/recipes/<%=recipes.id%>?_method=PUT" method="POST">
            <label for="ratings">Rating</label>
            <select name="ratings" id="ratings">
              <option value="" disabled selected>Please select ...</option>
              <% for (let i=5; i>0; i--) { %>
              <option value=<%= i %>><%= i %></option>
              <% } %>
            </select>
            <button type="submit">Rate Recipe</button>
          </form>
        </section>

        </section>
        <% }) %>



<%-include('../partials/_footer')%>